# Smart office blinds v2.0

## Prerequisites

Enable native mode https://console.cloud.google.com/firestore/data

```bash
yarn firebase login
yarn firebase init
```

Server:

```
pip3 install --user firebase-admin
```

To debug: 
`yarn run watch`
`yarn firebase serve`

To deploy:
`yarn firebase deploy`
